# Galax3D

A Streamlit application for querying and visualizing Gaia DR3 star data in 3D.

## Installation

1. Create and activate a virtual environment
2. Install dependencies:
   ```bash
   uv pip install -r requirements.txt
   ```

## Running the app

```bash
streamlit run galax_streamlit_app.py
```
